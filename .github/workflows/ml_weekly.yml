name: ml-weekly

on:
  workflow_dispatch:
  schedule:
    - cron: "0 12 * * 1"  # Lunes 12:00 UTC (ajustable)

jobs:
  fetch-ml:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run ML fetch
        run: python etl/ml_fetch_to_csv.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ml_weekly_csv
          path: data/ml_weekly.csv